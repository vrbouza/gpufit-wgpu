[project]
name = "gpufit-wgpu"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
#    "wgpu>=0.19.0",
    "webgpu>=1.1.0",
    "numpy>=1.24",
    "brimfile[remote-store]",
]

[project.optional-dependencies]
plot = ["matplotlib>=3.10.8"]

[tool.uv]
package = false
